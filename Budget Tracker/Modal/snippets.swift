//
//  snippets.swift
//  Budget Tracker
//
//  Created by Mikhailo Dovhyi on 14.11.2021.
//  Copyright © 2021 Misha Dovhiy. All rights reserved.
//

import UIKit

class snippets {
    //sort dictionary
    //let axises = boutle.exstamps.sorted(by: { (Double($0["enteredAmount"] ?? "0") ?? 0.0) - (Double($0["usedAmount"] ?? "0") ?? 0.0) < (Double($1["enteredAmount"] ?? "0") ?? 0.0) - (Double($1["usedAmount"] ?? "0") ?? 0.0) })
    /* func getColors() {
         
         
         var views:[ColorView] = []
         
         for i in 0..<colors.count {
             let new = ColorView.instanceFromNib() as! ColorView
             new.create(color: colors[i], selected: i == selectedColorId ? true : false)
             DispatchQueue.main.async {
                 new.frame = CGRect(x: self.colorViewSize * i, y: 5, width: self.colorViewSize, height: self.colorViewSize)
                 new.colorView.backgroundColor = colors[i]
                 self.colorScroll.addSubview(new)
             }
             views.append(new)
         }
         self.colorScroll.contentSize = CGSize(width: colors.count * colorViewSize, height: colorViewSize + 10)
         colorViews = views
         
     }
     */
}
